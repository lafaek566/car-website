<!DOCTYPE html>
<html lang="id">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Detail Mobil - <%= mobil.nama %></title>
    <style>
      /* Reset margin dan padding */
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      body {
        font-family: "Arial", sans-serif;
        background-color: #f7f8fa;
        color: #333;
        line-height: 1.6;
      }

      h1 {
        text-align: center;
        margin-top: 30px;
        font-size: 2.5rem;
        color: #333;
        text-transform: uppercase;
        letter-spacing: 1px;
      }

      h3 {
        font-size: 1.5rem;
        margin-bottom: 10px;
        color: #333;
      }

      p {
        font-size: 1.1rem;
        color: #777;
        margin-bottom: 20px;
      }

      /* Wrapper untuk detail mobil */
      .detail-container {
        max-width: 900px;
        margin: 0 auto;
        padding: 20px;
        background-color: white;
        border-radius: 10px;
        box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
        margin-top: 20px;
      }

      /* Gambar mobil */
      .detail-container img {
        width: 100%;
        height: auto;
        max-width: 600px;
        margin: 20px 0;
        border-radius: 10px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
      }

      /* Tombol kembali */
      .back-button {
        display: inline-block;
        margin-top: 20px;
        padding: 12px 20px;
        background-color: #4caf50;
        color: white;
        text-decoration: none;
        border-radius: 5px;
        font-size: 1.1rem;
        text-align: center;
        transition: background-color 0.3s, transform 0.2s;
      }

      .back-button:hover {
        background-color: #45a049;
        transform: scale(1.05);
      }

      /* Iklan */
      #adsense {
        margin: 30px 0;
        text-align: center;
        max-width: 728px;
        width: 100%;
      }

      /* Footer Styling */
      footer {
        background-color: #333;
        color: white;
        text-align: center;
        padding: 15px 0;
        margin-top: 40px;
      }

      footer p {
        font-size: 1rem;
      }
    </style>
  </head>

  <body>
    <!-- Detail Mobil Container -->
    <div class="detail-container">
      <h1>Detail Mobil: <%= mobil.nama %></h1>

      <!-- Spesifikasi Mobil -->
      <div>
        <h3>Spesifikasi:</h3>
        <p><%= mobil.spesifikasi %></p>
      </div>

      <!-- Gambar Mobil -->
      <div>
        <h3>Gambar:</h3>
        <img src="<%= mobil.gambar_url %>" alt="<%= mobil.nama %>" />
      </div>

      <!-- Menambahkan iklan di bawah detail mobil -->
      <div id="adsense">
        <script
          async
          src="https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js"
        ></script>
        <ins
          class="adsbygoogle"
          style="display: block"
          data-ad-client="ca-pub-XXXXXXX"
          data-ad-slot="XXXXXXX"
          data-ad-format="auto"
        ></ins>
        <script>
          (adsbygoogle = window.adsbygoogle || []).push({});
        </script>
      </div>

      <!-- Kembali ke daftar mobil -->
      <a href="/" class="back-button">Kembali ke Daftar Mobil</a>
    </div>

    <!-- Footer -->
    <footer>
      <p>&copy; 2025 Daftar Mobil. All rights reserved.</p>
    </footer>
  </body>
</html>
